function renderCalendar(){
 const list=DB.orders.filter(o=>o.date===state.calDate&&visOrder(o)&&o.status!=='canceled').sort((a,b)=>(a.t1||'').localeCompare(b.t1||''));
 return `
<div class="header"><h1>ðŸ“… ÐšÐ°Ð»ÐµÐ½Ð´Ð°Ñ€ÑŒ</h1><input type="date" value="${state.calDate}" onchange="state.calDate=this.value;render()" style="margin-left:auto;padding:6px;border-radius:6px;border:none"></div>
<div class="card">
 <div class="sec-title">Ð—Ð°Ð³Ñ€ÑƒÐ·ÐºÐ° Ð½Ð° ${state.calDate.slice(8)}.${state.calDate.slice(5,7)}</div>
 ${['08:00â€“10:00','10:00â€“12:00','12:00â€“14:00','14:00â€“16:00','16:00â€“18:00','18:00â€“20:00'].map(slot=>{
   const inSlot=list.filter(o=>o.t1<slot.split('â€“')[1]&&o.t2>slot.split('â€“')[0]);
   const busy=inSlot.filter(o=>o.worker);
   return `<div class="slot" style="${busy.length?'border-left-color:var(--red);background:#fef2f2':inSlot.length?'border-left-color:var(--blue);background:#f8faff':'border-left-color:var(--green);background:#f0fdf4'}">
    <b style="font-size:13px">${slot}</b>
    ${inSlot.length?inSlot.map(o=>`<div style="font-size:13px;margin-top:4px;cursor:pointer" onclick="go('details',${o.id})">
     ${o.worker?'ðŸ”´':'ðŸŸ¦'} ${o.t1}â€“${o.t2} â€¢ ${escapeHtml(o.client)} â€¢ ${escapeHtml(o.address)} ${o.worker?'â€¢ <b>'+escapeHtml(o.worker)+'</b>':''} <span class="badge" style="background:${ST[o.status].c};color:${ST[o.status].tc}">${ST[o.status].t}</span></div>`).join('')
    :'<div class="muted" style="margin-top:2px">ðŸŸ© ÑÐ²Ð¾Ð±Ð¾Ð´Ð½Ð¾</div>'}
   </div>`}).join('')}
</div>`}

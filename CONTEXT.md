# ServiceCRM — Контекст проекта

## Что это
Мобильное веб-приложение (PWA) для управления заявками в сервисной компании.
Рассчитано на 2-3 человека: оператор (принимает заказы), менеджер (осмотр/оценка), исполнитель (выполнение работ).

## Текущая версия
**v1.0.1** (последний деплой: 11 сентября 2026)

## Стек технологий
- **Фронтенд**: чистый HTML/CSS/JS в одном файле (index.html)
- **Хостинг**: GitHub Pages (https://shamgonov.github.io/Service-CRM/)
- **PWA**: manifest.json + service worker для установки на телефон
- **Realtime-обновления**: баннер появляется автоматически при новой версии

## Структура проекта
Service-CRM/
├── index.html # Основное приложение (все экраны в одном файле)
├── manifest.json # PWA манифест
├── sw.js # Service worker для офлайн-работы
├── icon.png # Иконка приложения
├── version.json # Текущая версия (x.y.z)
├── CONTEXT.md # Этот файл
├── tools/
│ ├── menu.bat # Точка входа в меню управления
│ ├── menu.ps1 # PowerShell-меню (основная логика)
│ ├── bump.ps1 # Инкремент версии
│ ├── patch.ps1 # Патч файлов (вшивает баннер обновлений)
│ ├── backup.bat # Скрипт бэкапов
│ └── deploy.bat # Скрипт деплоя
└── backups/ # Папка с бэкапами (создаётся автоматически)


## Версионирование
Формат: **x.y.z**
- z = счётчик деплоев (0-9)
- при z=10: z→0, y+1
- при y=10: y→0, x+1
- Пример: 1.0.9 → деплой → 1.1.0

## Система версионирования
Каждый деплой:
1. Автоматически увеличивает версию (bump.ps1)
2. Перепатчивает файлы с новой версией (patch.ps1)
3. Создаёт git коммит с сообщением "деплой vX.Y.Z"
4. Пушит на GitHub (force-with-lease)
5. Приложение у сотрудников само подхватывает через 30 сек (баннер "Обновление v...")

## Управление проектом
Запусти `tools\menu.bat` — откроется интерактивное меню:
- [1] Создать бэкап текущей версии
- [2] Список всех бэкапов
- [3] Восстановить из бэкапа
- [4] Патч приложения (баннер + realtime)
- [5] ДЕПЛОЙ GitHub (версия +1, коммит, пуш)
- [6] Деплой Firebase (не подключён)
- [0] Выход

## Текущий функционал приложения
- 5 экранов: авторизация, список заявок, детали заявки, создание заявки, календарь
- 4 роли: админ, оператор, менеджер, исполнитель (выбираются кнопками)
- Статусы заявок: новая, осмотр, согласована, в работе, ожидание материала, выполнено, оплачено
- Материалы: учёт с дедлайнами, статусами закупки, местом покупки
- Доп. работы: добавление позиций с пересчётом итога
- Фотоотчёт: обязательный/необязательный флаг
- Дележка прибыли: % для каждого работника
- Данные хранятся в localStorage браузера (каждое устройство независимо)

## Что НЕ реализовано (следующие шаги)
- Общая база данных для всех сотрудников (нужен Firebase/Supabase)
- Реальная авторизация (сейчас выбор роли кнопкой)
- Загрузка фото в облако
- Уведомления (push/telegram)
- Телефония
- Экспорт отчётов

## Коммиты
Все коммиты создаются автоматически с сообщением на русском: "деплой v1.0.2"
Git настроен на UTF-8: `git config --global core.quotepath false`

## Контакты
- Репозиторий: https://github.com/shamgonov/Service-CRM
- GitHub Pages: https://shamgonov.github.io/Service-CRM/
- Разработчик: Дмитрий

---
Последнее обновление: 11 сентября 2026
Версия документа: 1.0
---
## Журнал инцидентов и исправлений

### 11.09.2026 — Маркеры git-конфликта в index.html
- **Проблема**: при деплое с git pull возник конфликт слияния, маркеры <<<<<<< HEAD попали в файл и запушились на GitHub. Приложение открывалось криво.
- **Причина**: локальный репозиторий создавался заново через git init при уже существующем удалённом репозитории.
- **Фикс**: убран git pull из деплоя, используется только --force-with-lease. Добавлена проверка: деплой останавливается, если в index.html найдены маркеры конфликта.

### 11.09.2026 — Пустой index.html из-за here-string в терминале
- **Проблема**: вставка большого here-string с кириллицей прямо в терминал PowerShell привела к ошибке "SplattingNotPermitted" (интерпретация @var = @' как splatting), файл записался пустым.
- **Причина**: PowerShell-терминал некорректно парсит here-strings большого размера с UTF-8.
- **Фикс**: большие файлы создаются через VS Code (File → New File → вставить код → сохранить как UTF-8). Это единственный надёжный способ.

### 11.09.2026 — Кракозябры в bat-файлах
- **Проблема**: bat-файлы в UTF-8 читались cmd.exe в cp866, русский превращался в ?????.
- **Фикс**: menu.bat заменён на PowerShell-версию (menu.ps1) с корректным UTF-8 выводом через $OutputEncoding = [System.Text.Encoding]::UTF8.

### 11.09.2026 — Не устанавливается PWA на телефон
- **Проблема**: браузер не предлагал установку приложения.
- **Причина**: отсутствовал icon.png, manifest считался невалидным, service worker падал.
- **Фикс**: icon.png генерируется скриптом через System.Drawing (512x512, синий фон + "CRM").

### 11.09.2026 — Успешное восстановление после пустого index.html
- **Финальное решение**: файл index.html (37+ КБ) создаётся вручную через VS Code (вставка кода), а не через here-strings в PowerShell.
- **Причина**: PowerShell-терминал некорректно парсит большие here-strings с кириллицей, файл записывается пустым.
- **Правило на будущее**: большие файлы (>5 КБ) с кодом создаются только через VS Code. Автоматизация — для патчей, деплоя, бэкапов.

### 11.09.2026 — Мусор в строке APP_VERSION после патча
- **Проблема**: регулярка в patch.ps1 заменяла только часть строки, оставались лишние кавычки: `const APP_VERSION='1.0.5'; ';` → синтаксическая ошибка JS, приложение не запускалось (VS Code показывал "'; expected.").
- **Фикс**: регулярка заменена на полную строку: `const APP_VERSION=[^\r\n]*` → `const APP_VERSION='X.Y.Z';`. Мусор невозможен.

### 11.09.2026 — Нет иконки установки на телефоне
- **Проблема**: браузер открывает обычную страницу, пункта «Установить приложение» нет.
- **Причина**: отсутствовали icon.png / icon-192.png → manifest невалиден → service worker не ставился (404 в списке кэша) → браузер не считал сайт PWA.
- **Фикс**: иконки 192 и 512 px генерируются скриптом (System.Drawing), manifest обновлён с обеими иконками (purpose: any maskable).

### 11.09.2026 — НОВОЕ ПРАВИЛО: локальная проверка перед деплоем
- Любое изменение сначала проверяется локально: http://localhost:8000 (tools\server.ps1)
- Только после локальной проверки — деплой (menu.bat -> 5)
- Версия приложения теперь видна в шапке экрана входа («Версия v1.0.X»), изменения заметны сразу.

### 11.09.2026 — НОВОЕ ПРАВИЛО: локальная проверка перед деплоем
- Любое изменение сначала проверяется локально: http://localhost:8000 (tools\server.ps1)
- Только после локальной проверки — деплой (menu.bat -> 5)
- Версия приложения теперь видна в шапке экрана входа («Версия v1.0.X»), изменения заметны сразу.

### 11.09.2026 — Баннер обновлений: новая спецификация
- **Проблема**: баннер не показывался (старая логика реагировала только на несовпадение версии при открытой вкладке).
- **Новая логика**: каждый деплой увеличивает счётчик deploy в version.json. Приложение сверяет его с crm_ack в localStorage и показывает баннер ПРИ КАЖДОМ ОТКРЫТИИ, пока пользователь не нажмёт «Обновить».
- **Содержимое баннера**: текст из tools\release_notes.txt (контекст фиксов и решённых проблем); если файла нет — «Плановое обновление №N от дд.мм.гггг чч:мм» (N = номер деплоя).
- **Кнопки**: «Обновить» (применяет и перезагружает), «Обновить позже» (скрыть до следующего запуска).

### 11.09.2026 — Кракозябры в баннере + новый UX баннера
- **Причина кракозябр**: PowerShell 5.1 читает файлы без -Encoding как ANSI (cp1251), release_notes.txt был UTF-8 → текст в version.json битый.
- **Фикс**: во всех скриптах (menu.ps1, bump.ps1, patch.ps1) чтения файлов только с -Encoding UTF8.
- **Новый UX баннера**: тонкая зелёная полоска сверху (не перекрывает меню) → по нажатию модалка «Что нового?» с текстом фиксов → «Обновить» / «Обновить позже» (сворачивает в полоску). После применения обновления полоска исчезает и один раз показывается модалка «Что нового?» на русском с кнопкой «Понятно».

### 11.09.2026 — Кракозябры в модалке «Что нового?» (повторно)
- **Причины**: (1) release_notes.txt без BOM читался старой версией скрипта как ANSI; (2) деплой запущен из СТАРОГО открытого окна menu.bat — в памяти остался старый код.
- **Фикс**: release_notes.txt теперь пишется с BOM UTF-8 (читается верно любым кодом); menu.ps1 читает все файлы через ReadAllText(UTF8); в шапке меню указана версия скрипта (menu v4.1).
- **ПРАВИЛО**: перед деплоем ЗАКРЫВАЙ все окна menu.bat и открывай заново; проверяй что в шапке актуальная версия меню.

### 11.09.2026 — Кнопка установки приложения внутри приложения
- **Проблема**: на телефоне нет иконки/пункта установки. Причины: iOS не поддерживает автоустановку PWA (только вручную); на Android старый битый service worker блокировал критерии установки Chrome.
- **Фикс**: блок CRM-INSTALL в index.html: синяя кнопка «📲 Установить приложение» на экране входа. Android/Chrome — вызывает нативный диалог установки (beforeinstallprompt). iOS — модалка с инструкцией «Поделиться → На экран Домой». Кнопка скрывается после установки или в standalone-режиме.
- **Диагностика живого сайта**: manifest.json, sw.js, version.json валидны, текст обновлений на русском.

### 11.09.2026 — Ярлык открывал браузер вместо приложения
- **Причина**: ярлык создан через «Добавить на главный экран» (закладка), а не через нативный диалог установки PWA (beforeinstallprompt / «Установить приложение»).
- **Признаки настоящей установки**: WebAPK в списке приложений, сплэш-экран, полноэкранный режим без адресной строки, офлайн-работа.
- **Требование Chrome**: перед beforeinstallprompt нужно ~30 сек взаимодействия со страницей.
- **Следующий уровень (по запросу)**: APK через PWABuilder + проверка домена assetlinks.json через репозиторий shamgonov.github.io.

### 11.09.2026 — Маршрут APK (PWABuilder): состав пакета и подводные камни
- **Сборка**: pwabuilder.com → Package For Stores → Android. Архив «Google Play package» содержит: assetlinks.json, ServiceCRM.apk (установка на телефон), ServiceCRM.aab (для магазинов), signing.keystore + signing-key-info.txt (подпись).
- **ПРАВИЛО**: signing.keystore и signing-key-info.txt НЕ пушить в GitHub никогда. Резервная копия: E:\keystore-backup.
- **Домен**: создан репозиторий shamgonov.github.io (Public), запушен .well-known/assetlinks.json. Pages для user-репозитория включается: Settings → Pages → Deploy from branch → main / (root).
- **ЛОВУШКА**: Jekyll (включён в Pages по умолчанию) вырезает папки с точкой, поэтому .well-known отдавал 404. Фикс: пустой файл .nojekyll в корне репозитория shamgonov.github.io.
- **Проверка домена**: рабочая ссылка только https://shamgonov.github.io/.well-known/assetlinks.json (корень домена всегда 404 — это норма, index.html там нет).
- **Статус на момент записи**: APK собран, assetlinks запушен, ждём публикацию Pages и проверку домена Android-ом.

### 11.09.2026 — Сводка способов установки (итог исследования)
- Ярлык «Добавить на главный экран» = закладка, открывает браузер. Не приложение.
- WebAPK (нативный диалог Chrome) = полноэкранно, но ненадёжно: требует активного SW, GMS, ломается старым кэшем.
- TWA APK (PWABuilder) = настоящее приложение в списке приложений; контент живёт на GitHub Pages и обновляется деплоями menu.bat без пересборки APK; пересборка нужна только при смене иконки/имени/домена.
- iOS: автоустановки PWA не существует, только «Поделиться → На экран Домой»; для iOS актуальна кнопка-подсказка CRM-INSTALL в приложении.

### 11.09.2026 — APK установлен и работает
- Установка прошла через файловый менеджер (встроенная открывалка Telegram давала «не удалось обработать пакет»).
- TWA-приложение ServiceCRM работает: полноэкранный режим, иконка в списке приложений.
- Цикл разработки закрыт полностью: правка → патч → локальная проверка localhost:8000 → menu.bat 5 → баннер в браузере и в APK → «Обновить».
- Раздача сотрудникам: APK-файл либо GitHub Releases (по желанию).

### 11.09.2026 — Firebase + система доступа (этап 2)
- Подключён Firebase (Auth anonymous + Firestore). Общая база вместо localStorage: collection 'app'/doc 'state' — данные приложения; collection 'requests' — заявки на приём; collection 'employees' — одобренные сотрудники с ролью/квалификациями/правами.
- Привязка к устройству: deviceId в localStorage (crm_device).
- Владелец (разработчик): флаг crm_owner_7f3a в localStorage → вечный вход, переключение «тест как: Админ/Оператор/Менеджер/Работник», админка «Сотрудники».
- Поток найма: сотрудник открывает ссылку → «Запросить доступ» (имя) → заявка в админке владельца → «Принять» (выбор роли) / «Отклонить» → после одобрения приложение пускает в роль.
- Админка: список заявок, принятые сотрудники, выдача квалификаций (монтажник/диагност/электрик/старший), увольнение. Права по роли в PERMS.
- Конфиг: tools\fbconfig.js (compat-формат). ВАЖНО: ключи были в чате — для test-mode допустимо, позже ротировать.

### 11.09.2026 — ПРАВИЛО: все правки только скриптами терминала
- Запрещено присылать ручные правки в VS Code (кроме первичного создания больших файлов index.html/firebase-access.js).
- Любая правка кода доставляется PowerShell-скриптом, который сам читает/меняет/сохраняет файл.
- Фикс слоя доступа: владелец (флаг crm_owner_7f3a) теперь видит панель владельца вместо демо-ролей (window.__accessMode=true в ветке isOwner).

### 11.09.2026 — Пункт меню [6] «Деплой Firebase» убран
- Причина: ошибка «Not in a Firebase app directory (could not locate firebase.json)» — пункт пытался деплоить Firebase Hosting, который не используется.
- Хостинг приложения: GitHub Pages (пункт 5). Firebase = только Firestore (база) + Auth (устройства).
- Пункт [6] теперь выводит справку вместо команды firebase deploy.

### 11.09.2026 — Автовладелец вместо ручных команд консоли
- Первое устройство, открывшее приложение при пустой базе, автоматически становится владельцем (meta/owner в Firestore + флаг crm_owner_7f3a).
- Ручные команды в консоли браузера больше не нужны.
- Чужие устройства после этого видят экран «Запросить доступ».

### 11.09.2026 — Самодиагностика слоя доступа
- Автофикс fbconfig.js: если содержал модульные import — пересоздаётся compat-версией.
- CRM-ERRCATCH: видимая красная полоса с текстом JS-ошибки на экране (вместо тишины).
- Принудительный показ панели владельца через 2.5 сек при __accessMode без роли.

### 11.09.2026 — SDK Firebase перенесён локально
- Подозрение: gstatic.com недоступен из сети РФ → слой доступа умирал до старта.
- Фикс: firebase-app/auth/firestore-compat.js скачаны в tools/ и подключаются локально (index.html: src="tools/...").

### 11.09.2026 — Восстановление firebase-access.js
- Проблема: firebase-access.js содержал 520 байт — только хвостовые патчи, основной код (renderAccess, ownerPanel, checkApproved) был потерян в процессе экранирования PowerShell.
- Фикс: файл переписан через single-quoted here-string (без backticks/template literals) — без интерполяции, без экранирований. JS без template literals, обычная конкатенация.
- fbpatch.ps1 пересоздан: подключает локальный SDK (tools/*.js) и firebase-access.js перед </body>.

### 11.09.2026 — Видимая диагностика CRM-DBG
- Чёрная полоса сверху страницы: печатает статус слоёв (firebase/auth/accessJs/mode/ownerFlag/role) и перехватывает JS-ошибки с номером строки.
- Цель: по скрину полосы точно видеть, какой слой доступа умер, без консоли браузера.

### 14.09.2026 — ReferenceError dev in ownerPanel
- **Проблема**: панель владельца падала с Uncaught ReferenceError: dev is not defined, приложение откатывалось на демо-вход
- **Причина**: переменная dev не объявлена внутри ownerPanel()
- **Фикс**: var dev=deviceId() первой строкой ownerPanel; ревизия областей видимости во всём firebase-access.js; CRM-DBG удалён из прода

### 14.09.2026 — Закрытие этапа: безопасность + функциональность
**Закрыто:**
- P0 Firebase-слой: подключение FIREBASE-ACCESS (app/firestore/auth-compat + fbconfig + initializeApp), ReferenceError dev в ownerPanel устранён.
- P0 Деплой-затирание: patch.ps1 вырезает только CRM-UPDATER, слой переживает деплой; sw.js network-first для index.html.
- P0 Firestore Security Rules: firestore.rules + firebase.json + .firebaserc, задеплоены (firebase deploy --only firestore:rules), test-mode закрыт.
- Функция: takeOrder → status='approved' (кнопка «Приступил» достижима), setStatus требует фотоотчёт для completed/paid.
- setDur: сохраняет минуты t1, t2 не меньше t1 (кап 23:59).
- XSS: escapeHtml() во всех точках рендера (клиенты, адреса, описания, сотрудники, шаблоны, доп. работы, материалы); esc() в firebase-access.js.
- matState: без даты → «без срока» вместо NaN. renderCalendar: безопасная сортировка без t1.
- Гигиена: backups/, *.bak, deploy.bat/backup.bat (git pull без --force-with-lease, chcp 866) удалены из git; .gitignore расширен.
- Автоматизация: fbpatch.ps1 вставляет блок перед CRM-UPDATER; patch.ps1 обновляет «Версия vX.Y.Z» в шапке и пишет sw.js с network-first; server.ps1 порт 8000.

### 17.09.2026 — PIN-гейт распространён на владельца; logout сбрасывает crm_unlocked
- **Дыра 1**: владелец (isOwner) попадал на панель сразу — профиль не читался, PIN не спрашивался. **Фикс**: единая функция gateThenStart() — перед любым входом (панель владельца, тест-карточки ролей, приложение сотрудника) читается employees/{deviceId} (при отсутствии создаётся для владельца) и при установленном profile.pin без sessionStorage.crm_unlocked показывается PIN-гейт ДО панели владельца. Тест-карточки enterAs доступны только после разблокировки сессией. Дополнительно window.render содержит защиту: PIN без crm_unlocked перехватывает любой рендер.
- **Дыра 2**: logout не сбрасывал разблокировку — после «выйти» PIN не спрашивался в той же вкладке. **Фикс**: logout удаляет sessionStorage.crm_unlocked и сразу показывает PIN-гейт (если PIN установлен).
- **UX**: при неверном PIN показывается «Неверный PIN», после 3 попыток — блокировка без дальнейших вводов.

## ДОЛГИ
- Фильтрация и назначение заявок по специализациям.
- График платежей (плановые даты оплат) — сейчас платежи фиксируются по факту.
- Интеграция с бухгалтерией (экспорт платежей).
- Чек/QR для клиента по оплате.
- True-push уведомления (FCM + Cloud Functions onCreate → topic staff): нужен Blaze-тариф проекта service-crm-9f785 — деплой functions отклонён. После апгрейда: функция + firebase-messaging-compat.js + токен в employees/{deviceId}.fcmToken.
- Создать бакет Firebase Storage (консоль → Storage → Get Started) и выполнить firebase deploy --only storage:rules (storage.rules готовы в репо).

### 15.09.2026 — Личный кабинет «Мой профиль» + PIN-защита устройства
- **Модель**: employees/{deviceId}.profile = {fio,phone,email,city,schedule,avatar,pin,spec[]}; корневые role/quals/status не трогаются. Аватар — canvas-даунскейл 160px, JPEG 0.8, base64 в profile.avatar (без Storage). Владелец без документа — создаётся при первом открытии профиля (role:'admin', owner:true, status:'approved').
- **Экран «Мой профиль»**: пункт «👤 Профиль» добавляется в нижнее меню всем ролям (addProfileNavItem в слое доступа, не трогая NAVS index.html). Поля: ФИО/телефон/e-mail/город/график, специализация чипами (QUALS_CATALOG), аватар с превью, PIN (установка/изменение/сброс). «Сохранить» — update только поля profile своего документа. Все строки через esc().
- **PIN-вход**: после checkApproved читается profile.pin; если PIN есть и в sessionStorage нет crm_unlocked — экран ввода PIN (4 цифры). 3 неверные попытки → «Устройство заблокировано. Обратитесь к владельцу». sessionStorage очищается при закрытии вкладки. PIN-экран перехватывает старт до loadCloud.
- **Админка (Сотрудники)**: «👤 Профиль» — просмотр профиля сотрудника (read-only), «🔑 Сбросить PIN» — очищает profile.pin.
- **Совместимость**: поток владелец/запрос/приём, вкладки админки и can() не затронуты; node --check OK.

### 15.09.2026 — Админка ролей и прав (модель roles)
- **Модель**: коллекция Firestore `roles` — документ на роль {id, name, perms:[...], builtin:bool}. При первом открытии админки, если коллекция пуста, сидируются 4 встроенные роли (admin/operator/manager/worker) из BUILTIN_ROLES; у встроенных builtin:true. employees.role хранит id роли (старые admin/operator/manager/worker совпадают — миграция не нужна).
- **Каталог прав**: PERMS_CATALOG (12 ключей: orders_view/create/edit/status/delete, calendar, tasks, shopping, reports, admin_templates, staff_manage, roles_manage) — синхронизирован в firebase-access.js.
- **UI (экран staff)**: три вкладки — Заявки | Сотрудники | Роли. Селекты ролей берутся из коллекции roles (не хардкод). У сотрудника показ строки прав роли. Вкладка Роли: матрица чекбоксов по PERMS_CATALOG (сохранение в Firestore сразу), «＋ Создать роль», «Удалить» только у builtin:false; при удалении занятой роли — «Сначала переведите сотрудников на другую роль»; admin защищён (надпись «все права», чекбоксы скрыты).
- **Применение**: can(perm) — владелец всегда true, сотрудник по perms роли (кэш ROLES + ME.perms). Обёртки в слое доступа: guard go() по экранам, wrapGuard saveOrder/setStatus/deleteOrder и др. («Нет прав»), applyPermsUI() скрывает nav-пункты и кнопки создания/удаления без права. Навигация фильтруется по правам.
- **Совместимость**: поток владелец→панель, сотрудник→запрос→приём не тронут; старые строковые роли работают. Все строки через esc()/escapeHtml().
- **firestore.rules**: добавлена коллекция roles (read:true, write: request.auth!=null) — иначе админка не читает/не пишет роли.
- **ВНИМАНИЕ**: после этой правки ОБЯЗАТЕЛЬНО задеплоить правила (firebase deploy --only firestore:rules) и приложение (menu.ps1 5). Правила roles нужны, иначе сид ролей упадёт с permission-denied.

### 17.09.2026 — Профиль доступен всем + мобильный навбар
- **Права**: пункт «👤 Профиль» доступен всем ролям без настройки прав: addProfileNavItem() добавляет его вне can(), в applyPermsUI пункт с data-profile-nav принудительно показывается (el.style.display=''). В PERMS_CATALOG добавлен ключ profile_view (для матрицы ролей, на доступ не влияет).
- **Мобильная версия**: в index.html добавлены @media (max-width:380px) и (max-width:330px) — на узких экранах (iPhone SE 320px) паддинги и шрифты nav-item уменьшаются, 6 пунктов навбара помещаются без переполнения.
- **Совместимость**: node --check OK; can()/SCREEN_PERM не изменены — профиль не требует права.

### 17.09.2026 — Демо-вход удалён; запомненный профиль устройства
- **Демо-вход**: карточки «Дмитрий/Оля/Анна/Вася» в index.html заменены на заглушку «Загрузка...» — слой доступа всегда перехватывает рендер (window.__accessMode), демо-экран не показывается ни при каком пути. Тест-карточки «тестировать как…» — только в ownerPanel() (isOwner()). При сетевой ошибке Firestore (code unavailable) вместо входа — «Нет соединения с сервером, проверьте интернет» + кнопка «Повторить».
- **Запомненный профиль**: при входе сотрудника startMain пишет localStorage crm_last_user={name,role,avatar}. logout (не владелец) показывает карточку «Войти как {имя}» (аватар+роль) → restoreLastUser перечитывает employees/{deviceId}: approved → вход (при profile.pin — сначала PIN-гейт), иначе — очистка и форма запроса. Кнопка «Запросить новый доступ» чистит crm_last_user и показывает форму. Старт не-одобренного устройства тоже очищает crm_last_user. Владелец при logout видит панель 👑.
- **Совместимость**: все строки через esc(); приём заявок, админка, can(), PIN-гейт не затронуты; node --check OK.

## КОНТРОЛЬНЫЙ ЧЕК 17.09.2026
| Блок | Статус |
|---|---|
| 1. Доступ и устройства | OK: панель владельца + тест-роли (только isOwner), форма запроса, «Войти как…»/«Запросить новый доступ», офлайн-экран «Нет соединения» вместо демо-входа, демо-карточки удалены (grep=0) |
| 2. Роли и права | OK: вкладки Заявки/Сотрудники/Роли, создание/удаление кастомных, запрет удаления занятой, admin защищён, матрица в Firestore, can()+applyPermsUI+guard, сид устойчив к permissions-ошибке (fallback BUILTIN_ROLES) |
| 3. Профиль и PIN | OK: профиль+аватар (canvas 160px JPEG base64) в employees/{deviceId}.profile — переживают F5; PIN-гейт для всех (включая владельца, gateThenStart), 3 попытки → блокировка, logout сбрасывает crm_unlocked, сброс PIN в админке; профиль всем без прав (data-profile-nav вне can()) |
| 4. Ядро | ИСПРАВЛЕНО: matAct «Не найти» больше не уводит completed/paid-заявку в waiting (только пометка материала + предупреждение). OK: «Приступил» только из approved+worker; фотоотчёт обязателен для completed/paid; сортировка календаря не падает без t1; matState без NaN при пустой date; escapeHtml/esc по всем пользовательским строкам |
| 5. PWA и деплой | ИСПРАВЛЕНО: sw.js (шаблон patch.ps1) — network-first, не кэширует !resp.ok (404), игнорирует чужие origin. OK: версии version.json=APP_VERSION=шапка согласованы; маркеры CRM-UPDATER/FIREBASE-ACCESS/CRM-INSTALL есть, CRM-DBG нет; .gitignore актуален, backups в git нет; правила покрывают app/requests/employees/roles/meta + catch-all deny |
| Требует решения владельца | Firebase SDK (tools/firebase-*-compat.js) в git — осознанно (иначе APK/PWA без CDN не работает), при переезде на CDN — удалить из репо; firestore.rules задеплоены (этап №16), с тех пор не менялись — передеплой не требовался |

### 17.09.2026 — Реальные фото (Firebase Storage)
- **Storage**: storage.rules (orders/{orderId}/* и avatars/{deviceId}.jpg: read all, write auth + <5МБ + image/*) + секция storage в firebase.json. Деплой правил НЕ УДАЛСЯ: бакет Storage не создан (нужен «Get Started» в консоли Firebase → владелец). Правила будут применены при первом деплое после создания бакета.
- **Фото в заявке**: input capture=environment → canvas-даунскейл 1280px JPEG 0.7 → Storage orders/{orderId}/{ts}.jpg → URL в order.photos[]. Галерея миниатюр, полноэкранный просмотр, удаление (владелец/админ). Фолбэк при недоступном Storage: base64 в документ, лимит 3 фото, с предупреждением. Старые заглушки «🖼» рендерятся как раньше.
- **Аватары**: новые — Storage avatars/{deviceId}.jpg (URL в profile.avatar), старые base64 читаются как раньше.
- **Права**: добавление фото — can(orders_edit) или исполнитель заявки; удаление — владелец/админ.
- SDK: tools/firebase-storage-compat.js 10.12.5 подключён после auth, до fbconfig.

### 17.09.2026 — CSV-экспорт отчётов
- Кнопка «⬇ CSV» на экране Отчёты (право reports): одним файлом две таблицы — свод по работникам (имя, заявок, сумма, доля, к выплате) и журнал заявок (дата, клиент, телефон, адрес, статус, сумма, исполнитель), сортировка по дате убыв.
- Формат: BOM \uFEFF, разделитель «;», даты ДД.ММ.ГГГГ, кавычки/точки-с-запятой в тексте экранируются по RFC, файл otchet_GGGG-MM-DD.csv через Blob+URL.createObjectURL (работает в мобильном APK). Функциональный тест в node: BOM=true, кириллица и экранирование корректны.

### 17.09.2026 — Уведомления о новых заявках (in-app)
- **Вариант: in-app realtime** (true-push FCM требует Blaze-тариф — проверено попыткой деплоя functions, отказ; записано в ДОЛГИ).
- Механика: onSnapshot app/state → при появлении заявок (по id) у сотрудников с правом orders_view (не владелец): красный бейдж-счётчик (фикс. правый верх), звук-бип WebAudio 880Гц (без файлов), navigator.vibrate(200). Бейдж гасится при открытии списка заявок (go(orders)). Первый снапшот и правки в первые 3с после старта не считаются новыми (отсечение своих правок/первичной загрузки).

### 17.09.2026 — Календарь без конфликтов
- saveOrder перед сохранением проверяет пересечение интервала t1–t2 с другими заявками того же исполнителя в тот же день (кроме редактируемой; активные статусы). При пересечении — модалка «Исполнитель занят ЧЧ:ММ–ЧЧ:ММ (клиент)»: «Всё равно назначить» только владелец/админ (isOwner/can(orders_edit)), остальным — только «Отмена».
- Календарь: слоты с назначенным исполнителем подсвечены красным (🔴+красная рамка), назначённые без исполнителя — синим, пустые — зелёным «🟩 свободно».
- Логика протестирована в node (vm): пересечение/другой день/skipId/граничное касание/права override — 7/7 OK.

## ФИНАЛЬНЫЙ КОНТРОЛЬНЫЙ ЧЕК 18.09.2026 (блоки А–Г)
| Блок | Статус |
|---|---|
| А. Фото/Storage | КОД OK: SDK подключён, uploadPhoto+фолбэк base64 (лимит 3), галерея/просмотр/удаление, аватары→avatars/{deviceId}.jpg. ПРАВИЛА готовы (storage.rules+firebase.json), деплой отложен: нет бакета Storage (решение владельца). HTTP 200 на SDK |
| Б. CSV | OK: exportCsv, 2 таблицы, BOM/;/ДД.ММ.ГГГГ, Blob-скачивание; node-тест корректности пройден |
| В. Уведомления | OK (in-app): onSnapshot, бейдж+бип+вибрация при новых заявках (orders_view, не владелец), гашение при открытии списка. True-push FCM — ДОЛГ (нужен Blaze) |
| Г. Конфликты календаря | OK: findConflicts+модалка (override только владелец/админ), подсветка слотов (исполнитель—красный, без исполнителя—синий, свободно—зелёный); node-тесты 7/7 |
| Целостность | OK: версии 1.3.4 согласованы (version.json=APP_VERSION=шапка=sw.js), маркеры целы, конфликтов 0, node --check OK, git чист, HEAD=origin/main (1461d37, деплой №25) |

### 18.09.2026 — Фото в коллекции photos (без Blaze); лимит 8; путь миграции в Storage описан
- **Коллекция photos**: документ {orderId, data (base64 JPEG ≤~900КБ), ts, by=deviceId}, id={orderId}_{ts}. uploadPhoto: даунскейл 1000px/0.65, если base64 > 900 КБ — повторный 800px/0.6. Лимит 8 фото на заявку (учитывает и старые фото в документе).
- **Галерея**: двойное чтение — photos where orderId== (сорт. по ts) + старые o.photos из документа; просмотр на весь экран; удаление = delete документа (клиентская проверка: владелец/админ или автор by; в rules delete — auth!=null, т.к. deviceId анонима недоступен в token — серверная проверка автора в ДОЛГАХ).
- **photoReport** для completed/paid: orderHasPhotos — документы photos по orderId ИЛИ старые o.photos.
- **Миграция в Storage**: ветка сохранена; при старте checkStorage — пробная запись .probe/check.txt; если бакет появился — новые фото в Storage (URL в data), старые читаются из photos. Аватары — без изменений (base64 160px).
- **firestore.rules**: match /photos/{id} (read: all; write: auth!=null) — ЗАДЕПЛОЕНЫ (18.09).

### 18.09.2026 — Миграция app/state → коллекция orders
- **Схема**: заявки — по документу в коллекции orders (id=String(order.id), поля заявки целиком; o.photos — legacy-поле старых заявок, новые фото в photos). Чтение: onSnapshot по orders (сорт. по id) + onSnapshot по app/state (users/templates/seq, БЕЗ orders) — любой снапшот → render().
- **Запись**: мутации заявок (статусы, взятие, завершение, материалы, доп.работы, удаление фото из документа) → markOrder(o)+save() → orderSave(o) — только документ заявки; настройки (setShare, addTpl, delTpl, owner-сид) → saveSettings() — app/state без orders. doSaveOrder: orderSave + saveSettings (seq). Аудит всех 15 save() проведён.
- **Миграция** (одноразовая, только владелец, при startMain): если app/state.db.orders непуст и флага ordersMigrated нет, а коллекция orders пуста — батч чанками по 400, затем app/state перезаписывается без orders + ordersMigrated:true. Если в orders уже есть данные — только флаг. Идемпотентна (node-тест: повторный запуск — 0 записей).
- **Фолбэк**: при ошибке чтения orders — legacy app/state.db.orders read-only + жёлтый баннер «режим только чтения»; попытки записи — предупреждение.
- **firestore.rules**: match /orders/{id} (read: all; write: auth!=null) — ЗАДЕПЛОЕНЫ (18.09).

## ИНЦИДЕНТ 18.09.2026 — деплой с битым инлайн-скриптом
- **Что случилось**: деплой №26 v1.3.5 (фото в photos) и №27 v1.3.6 (миграция orders) содержали синтаксическую ошибку в инлайн-скрипте index.html (шаблонная строка галереи photoGrid — правка вносилась без синтаксической проверки инлайн-блоков; node --check прогонялся только по firebase-access.js). Прод был нерабочим: Uncaught SyntaxError, DB is not defined.
- **Откат**: на деплой №25 v1.3.4 (1461d37) — последний коммит с чистыми инлайн-блоками (4/4 OK). Уточнение: предложенный владельцем 7b80aff (№26) сам содержит тот же битый фрагмент — откат выполнен на №25. Деплой отката — №27 (bump сам поднимет версию).
- **Гейт**: tools/check-inline.js — извлекает все инлайн-<script> из index.html и прогоняет через vm.Script; exit 1 = деплой запрещён. Встроен ОБЯЗАТЕЛЬНЫМ шагом в menu.ps1 [5] после patch.ps1 (вывод INLINE_OK N).
- **Причина брака**: правка инлайн-скрипта index.html без синтаксической проверки. Теперь любой деплой через menu.bat 5 невозможен с битым инлайн-кодом.
- **Миграция orders**: приостановлена; возобновить только после рабочего отката и с включённым гейтом, разбив правку на проверяемые шаги.

### 18.09.2026 — ЭТАП 1: возвращены фото в коллекции photos (после отката аварии)
- Реализовано заново с исправленным синтаксисом (причина аварии №26 — вложенная шаблонная строка в галерее; теперь конкатенация строк, гейт INLINE_OK 4 пройден).
- uploadPhoto: даунскейл 1000px/0.65, при base64 > 900КБ — повторный 800px/0.6; лимит 8 фото (учёт legacy o.photos); коллекция photos {orderId,data,ts,by}, id={orderId}_{ts}; Storage-ветка с пробной записью .probe/check.txt при старте (если бакет появится — новые фото в Storage).
- Галерея: двойное чтение (photos where orderId + legacy o.photos), просмотр на весь экран, удаление (владелец/админ/автор по by; legacy — из массива). orderHasPhotos для completed/paid. window.isOwner/window.deviceId экспортированы.
- Правила photos не трогались (задеплоены ранее).

### 18.09.2026 — ЭТАП 2: миграция orders (повторно, через гейт)
- Реализовано заново после отката: коллекция orders (id=String(order.id)); чтение — два onSnapshot (orders сорт. по id + app/state без orders), любой снапшот → render(). Запись: мутации заявок → markOrder(o)+save() → orderSave (только документ); настройки (setShare/addTpl/delTpl/owner-сид/seq) → saveSettings (app/state без orders). Аудит 10 точек мутаций заявок + 3 настройки.
- Миграция: только владелец при startMain; app/state.db.orders → orders батчами по 400; затем app/state без orders + ordersMigrated:true; если в orders уже есть данные — только флаг. Node-тесты: миграция 3 заявок OK, идемпотентность OK, маршрутизация save(): правка заявки НЕ пишет app/state (0 записей), настройки пишутся (1 запись).
- Фолбэк: ошибка чтения orders → legacy app/state.db.orders read-only + жёлтый баннер «режим только чтения».
- Правила orders не трогались (задеплоены ранее). Гейт INLINE_OK 4 пройден перед деплоем.

### 18.09.2026 — boot-guard: самовосстановление битого SW-кэша без очистки данных
- **index.html** (первый инлайн-скрипт, до SDK): window.onerror на этапе загрузки; фатальная ошибка (источник не firebase) при отсутствии sessionStorage crm_recovered → флаг + unregister SW + удаление ВСЕХ кэшей (caches.keys→delete) + location.reload(). Вторая попытка не делается (флаг стоит) — экран «Ошибка загрузки, проверьте интернет» с кнопкой перезагрузки. Ошибки в SDK не считаются фатальными.
- **sw.js** (шаблон patch.ps1): activate — удаление чужих кэшей + clients.claim() (было). fetch: navigate — ВСЕГДА только сеть (обновление кэша при ok), fallback — инлайн-офлайн-страница «Нет соединения» (без кэш-fallback). Прочие GET — сеть→кэш как раньше.
- Симуляция в node (vm): 1-я фатальная ошибка → 1 перезагрузка, 2 кэша удалены, флаг=1; 2-я ошибка → без действий; ошибка SDK → без действий. 7/7 OK.

### 18.09.2026 — PIN-хэш (PBKDF2) + серверные права в firestore.rules
- **PIN**: вместо открытого profile.pin — пара {pinSalt, pinHash}: PBKDF2-SHA-256 через crypto.subtle, 100 000 итераций, соль 16 байт (randSalt). Гейт (pinSubmit→verifyPin), установка/смена (savePin→makePinRecord), сброс (resetMyPin/adminResetPin — чистят оба поля) работают через хэш. Мягкая миграция: старый открытый pin при первом успешном вводе гейта пересохраняется в {pinSalt,pinHash}, поле pin удаляется. Фолбэк: crypto.subtle недоступен (старый WebView) → открытый pin + жёлтое предупреждение в профиле (ДОЛГ остаётся). Тест PBKDF2 в node (webcrypto): детерминированность, разные соли → разные хэши, неверный PIN, длины 64/32 hex — 5/5 OK.
- **Серверные права (uid-привязка)**: при signInAnonymously клиент пишет employees/{deviceId}.uid = auth.uid и usermap/{uid}={deviceId} (create/update только своим uid). Правила задеплоены (Deploy complete): employees update — своему устройству только profile+uid (affectedKeys().hasOnly), create/delete — владелец/админ (+bootstrap владельца при пустом meta, request.resource.data.owner==true); roles write — только владелец/админ; photos delete — автор (by через usermap) или владелец/админ; orders/app/state/requests/meta — как раньше; catch-all deny. isOwner = meta/owner.uid == auth.uid (meta/owner теперь пишет uid при записи). Проверка матрицы правил структурно — 11/11 OK.
- Из ДОЛГОВ сняты: «хэширование PIN», «жёсткие rules по auth.uid + серверная проверка автора фото».

### 19.09.2026 — Офлайн-режим с очередью (крупный долг закрыт)
- **Кэш чтения**: каждый снапшот orders и app/state пишется в localStorage (crm_cache_orders, crm_cache_state). При старте без сети — loadCachedData() сразу, баннер «Офлайн: показаны сохранённые данные».
- **Очередь записей**: офлайн-мутации заявок (orderSave), фото (base64) и настройки складываются в crm_queue с меткой времени; локально применяются сразу (optimistic). Баннер «Офлайн: изменений в очереди — N». При восстановлении сети (online-событие) — flushQueue: последовательная отправка (orderSave по id / photos.set / saveSettings), затем очистка и зелёный «Синхронизировано» 3 сек. Конфликты: серверный снапшот побеждает локальную копию, очередные патчи применяются поверх.
- **sw.js** (шаблон patch.ps1): navigate-фолбэк — закэшированный последний успешный index.html (если есть), страница «Нет соединения» — только когда кэша нет вовсе.
- **Boot-guard**: сетевые ошибки (Failed to fetch, NetworkError, ERR_*) не считаются фатальными — штатный офлайн не триггерит чистку кэша.
- Симуляция в node (vm): офлайн-мутации (2 заявки+фото+настройки) → очередь 4, счётчик в баннере, optimistic-применение, в сеть не уходит; «сеть» → flush → очередь пуста, настройки отправлены, зелёный баннер; перезагрузка офлайн → кэш загружен (заявки, seq). 12/12 OK.
- Из ДОЛГОВ сняты: «офлайн-кэш orders», «offline-очередь».

### 19.09.2026 — Уведомления этап 1 (без Blaze)
- **Движок событий**: на каждом снапшоте orders сравнение с предыдущим: назначение мне-работнику, переназначение/перенос (postponed), завершение (completed → владельцу/orders_view), материал «не найти» (→ shopping), изменение календаря моей заявки (date/t1/t2). Новые заявки на доступ (requests, pending) — владельцу. Просрочка — локальная проверка раз в минуту (активная заявка с прошедшим t2), один раз на заявку (флаги crm_overdue_fired), мне-исполнителю и владельцу. Подавление автора: orderSave пишет lastBy=deviceId, свои правки не уведомляют. Отсечение первого снапшота и 3с после входа/правок (LAST_ORDER_TS).
- **Хранение**: {type,title,body,orderId,ts,read} в crm_events (последние 50).
- **Каналы**: in-app красный бейдж на пункте меню (непрочитанные), бип 880Гц, вибрация; системные Notification (title/body/icon.png/tag=orderId_type, клик → focus+go details) при свёрнутом приложении; Badging API (setAppBadge/clearAppBadge). Notification API недоступен — только in-app, без ошибок.
- **Настройки в профиле**: тумблеры «Системные уведомления» (requestPermission при включении), «Звук», «Вибрация», поля «Не беспокоить с…по…» (часы; в DND — только бейдж). Хранение profile.notify (правила разрешают profile).
- **CAN**: событие показывается только при праве на экран (orders_view/shopping/calendar/staff_manage).
- Симуляция (node vm): назначение/перенос/завершение/материал → события; автор не уведомляется; просрочка один раз; бейдж сходится с непрочитанными; прочтение по экрану; DND без бипа; без права событие не создаётся. 12/12 OK.
- В ДОЛГАХ остаётся: «FCM-пуши (Blaze) — уведомления при убитом приложении».

### 19.09.2026 — Схема выполнения задачи (маршрут точками)
- **Данные**: order.route = массив {id, type supplier|client|base|custom, title, address, status pending|way|arrived|done, ts_arrived, ts_done, geo}. Автогенерация при takeOrder (approve): материалы с закупкой работником/доставкой — точка поставщика (адрес из m.where или «уточнить адрес»), клиент — адрес заявки, база — DB.baseAddress (поле в админке). Первая точка — status=way.
- **Экраны**: «Схема» (кнопка в карточке работника и в details) — вертикальный таймлайн: у текущей точки deep-links Яндекс/Google/2ГИС по адресу, «Прибыл» (ts_arrived + geo при разрешении геолокации), «Выполнено» (ts_done, следующая точка — way, событие ролям orders_view «{имя}: {точка} выполнено»). Прогресс «2/3 точек». «Весь маршрут» — Яндекс (rtext=A~B~C) и Google (dir/A/B/C). В details — тот же таймлайн read+edit (добавить/удалить/переставить, правка адреса) + «Лог движения» (время+гео).
- **События**: done — pushEvent со звуком; arrived — silent (бейдж без звука). Чтение route-событий — по экранам orders/details/tasks.
- **Права**: «Прибыл/Выполнено» — исполнитель заявки или orders_edit; редактирование точек — orders_edit.
- **Совместимость**: заявки без route — временный таймлайн из одной клиентской точки без записи.
- Симуляция (node vm): генерация (поставщик по worker-закупке, клиент, база; manager-закупка не попадает), статусы, прогресс, гео, события, silent, deep-links, лог — 16/16 OK.
- **ДОЛГ добавлен**: «визуализация лога движения на карте (нужен ключ Яндекс Карт)».

### 19.09.2026 — убрана legacy-заглушка 🖼 из src галереи и просмотра: img только на реальные данные (data:image/ или http), иначе плейсхолдер «📷 нет фото».

### 20.09.2026 — Типы заявок, этапы и платежи (новая бизнес-модель)
- **Поля заявки**: type standard|manufacture|service (default standard, старые заявки работают как standard); stages — только для manufacture (автогенерация замер→изготовление→монтаж при выборе типа в форме; сервисный — один этап «Выезд»); payments — массив {id,sum,type advance|partial|full|materials,method cash|card|transfer,date,note}. Вычисляемые: totalPaid (приходы, без materials), materialsPaid, balance=total-totalPaid.
- **UI**: в форме — «Вид заказа» с секцией этапов (плановые даты, исполнитель этапа из сотрудников); в details — таймлайн этапов (для manufacture вместо «Приступил/Завершить» — кнопки этапов; finishOrder блокируется при неготовых этапах) и секция «Платежи» (read всем, write owner/admin/finance_edit — новое право в PERMS_CATALOG): «+ Приход» (аванс/частичный/полный) и «+ Выдать на материалы» через модалку. В tasks — таймлайн этапов в карточке.
- **Связка материалы↔аванс**: при «Прибыл» на точке поставщика (схема маршрута) — модалка «Подтвердить сумму закупки», списание с выданного аванса, остаток возвращается (запись в payments с note).
- **Админка → Финансы**: все платежи за период (фильтр по датам), свод приходы/расходы/касса. CSV расширен: секция «ПЛАТЕЖИ» (дата/заявка/клиент/тип/метод/сумма/примечание + итоги).
- **События**: «этап готов» — next-исполнителю (если назначен); «платёж получен» / «аванс на материалы выдан» — owner/admin.
- **Правила**: payments/stages — часть документа orders (write по правилам orders); правила задеплоены. Серверные права не тронуты.
- Симуляция (node vm): дефолтные этапы по типам, totalPaid/materialsPaid/balance, готовность этапа + событие, совместимость со старыми заявками, canFinance — 13/13 OK.
- **ДОЛГИ добавлены**: «график платежей (плановые даты оплат)», «интеграция с бухгалтерией», «чек/QR для клиента».

### 20.09.2026 — Гибкие этапы, скролл, кассовое закрытие
- **Блок 1 — этапы**: редактирование названия инлайн (карандаш → prompt, stages[i].title) у orders_edit; срок этапа — dueMode date|ready|agree (селект в форме создания и правка в details), plan_ready только при date; в таймлайне — дата, «по готовности» или «по договорённости». Календарь/конфликты этапы не учитывают (у этапа нет слотов t1/t2).
- **Блок 2 — скролл**: render() сохраняет scrollY и фокус (id + позиция курсора) до перезаписи DOM и восстанавливает через requestAnimationFrame — снапшоты и таймер просрочки больше не сбрасывают прокрутку. Холостые снапшоты (deep-equal JSON предыдущих orders/state) не вызывают render вовсе.
- **Блок 3 — касса**: «Аванс: X / Остаток: Y» в paymentsBlock (Y = total с доп. работами − totalPaid; доп. работы уже входили). «Завершить заказ» при остатке > 0 — модалка «Закрытие заказа»: состав работ, итог, оплачено, остаток; «Заказчик внёс остаток» (платёж full, note «при закрытии»), «Оплачено полностью» (только если платежей не было), «Отмена». После платежа — проверка фотоотчёта → completed; событие owner/admin «платёж получен» (виден в Финансах и CSV). Баланс-бейдж в списке — по той же формуле.
- Симуляции (node vm): скролл восстановлен (500), фокус+курсор (pf-fio@7), dueMode дефолты и лейблы, доп. работы в остатке (12000/5000/7000), платёж при закрытии + событие + баланс 0, условие «Оплачено полностью» — 11/11 OK.

### 20.09.2026 — фикс краха STOR is not defined (STOR=null при отсутствии Storage, try/catch на Storage-путь, фолбэк photos без ошибок) + убран плейсхолдер «📷 нет фото» из фото-блока (только миниатюры, пунктирная «＋» и строка-подсказка).

### 20.09.2026 — бейджи-счётчики привязаны к своим пунктам нижнего меню (position:relative у пункта, бейдж absolute top:-4px right:calc(50% - 22px) 18px/11px, распределение по типам событий: заявки→Заявки/Мои задачи, материалы→Закупки, календарь→Календарь, найм→Админ; сумма при нескольких типах).

### 20.09.2026 — настройка settings.storageEnabled (чекбокс в админке, owner/admin): проб Storage только при включённой настройке и раз за сессию; при false — ноль запросов к Storage, фото сразу в photos; путь загрузки — синхронно по кэшированному STOR_OK (проб убран из uploadPhoto); при включённой настройке без бакета — жёлтое предупреждение в админке.

### 21.09.2026 — Тестовые заявки, напоминания, отмена заказа
- **Тестовые заявки**: order.test (чекбокс «🧪 Тестовая заявка» в форме и details, owner/admin/finance_edit; существующие НЕ помечались автоматически); настройка settings.showTest в админке (default false). При showTest=false тестовые скрыты из списков заявок, «Моих задач», календаря, уведомлений и схемы маршрута; из отчётов, «Финансов», CSV и расчёта долей исключены ВСЕГДА (bizOrder). Бейдж «🧪 тест».
- **Напоминания** (в минутном таймере): «Скоро выезд» — исполнителю, заявки сегодня со стартом в [сейчас…+60 мин], один раз на заявку (crm_soon_fired); «Завтра N заявок, первая в {время}» — ежедневно после 18:00 (crm_tomorrow_fired). Уважают DND (через pushEvent) и orders_view; тестовые не напомиаются.
- **Отмена заказа**: новое право orders_cancel (PERMS_CATALOG «Отмена заявок», у owner/admin через all; редактируется в матрице ролей). Кнопка «❌ Отменить заказ» в details (и через селект статуса → модалка), недоступна для completed/paid. Модалка с обязательной причиной → status=canceled (в ST, серый бейдж «отменена», карточка серая), cancelReason/cancelBy/cancelTs; из календаря и «Моих задач» исчезает; событие «Заявка отменена: {причина}».
- Симуляции (node vm): скрытие тест-заявок (списки/календарь/задачи/CSV/отчёты) 14/14; напоминания: окно +60 мин, один раз, тестовые пропускаются, «Завтра» только после 18:00, без orders_view молчание — 9/9.

### 21.09.2026 — Лог истории заявки
- **Модель**: order.history — события {ts (ISO), by (deviceId|system), action, details}; при записи обрезается до 100 последних; сортировка по ts по убыванию при показе.
- **Хелпер logAction(order,action,details)** (index.html, глобален — используется и из firebase-access.js в uploadPhoto). Точки логирования: create (doSaveOrder), assign+status_change (takeOrder), status_change (setStatus, включая ветку фотоотчёта), payment_received/payment_materials (payAdd, closeOrderPay, closeOrderFull), stage_add (stageAdd), stage_complete (stageDone), cancel (doCancelOrder), photo_add (uploadPhoto после записи в photos), edit (addExtra/delExtra — доп. работы).
- **Просмотр**: секция «📜 История» в details после платежей; формат — действие + детали (histLine) + время + имя (userNameById: lookup по DB.users.deviceId, system→«система»). Видна только при orders_history (новое право «Просмотр истории заявок» в PERMS_CATALOG) или владельцу; без права секция скрыта, но лог пишется.
- **Совместимость**: старые заявки без history — «Событий пока нет»; первая мутация инициализирует массив.
- Симуляции (node vm): запись/обрезка до 100/cancel/payment/сортировка/право/совместимость — 10/10 OK (9-й изолированно).

### 21.09.2026 — Настраиваемый справочник специализаций
- settings.specializations (массив строк, едет в app/state: saveSettings/cacheState/state-снапшот); правка — owner/admin. Админка: список с использованием (N сотрудников), переименование (инлайн-инпут + ✓/Enter, обновляет профили), удаление с подтверждением «используют N сотрудников», добавление (дубли отклоняются).
- Профиль: чипы из справочника (selected → profile.specs, пишется также legacy spec для совместимости); пустой справочник — «Справочник пуст…»; значения вне справочника — серые чипы «вне справочника» с крестиком (в справочник не добавляются). Старые чипы-пресеты (QUALS_CATALOG монтажник/диагност/электрик/старший) удалены из кода; селект квалификаций в «Сотрудниках» теперь из справочника.
- ДОЛГИ добавлен: «фильтрация и назначение заявок по специализациям».
- Симуляции (node vm): добавление/дубль/удаление с подтверждением/переименование с обновлением профилей/вне справочника/пустой справочник/права — 10/10 (7-й подтверждён отдельно).

### 21.09.2026 — Контрольный чек: 3 бага исправлены
- **Баг 1 (нет специализаций в админке)**: причина — секция была только в экране «⚙️ Админ» (renderAdmin), а владелец работает через «👥 Управление» (screen=staff, renderStaff → вкладки Заявки/Сотрудники/Роли), где её не было. Исправлено: блок «🛠 Специализации» добавлен в конец вкладки «Роли» (tabRoles) с использованием (specCount), ✏️/🗑/＋; specAdd/specRename/specRenameSave/specDel после изменений обновляют текущий экран через refreshAdminView() (staff → loadStaff, иначе render). Секция в renderAdmin тоже осталась.
- **Баг 2 (лог не раскрывает события)**: UI истории переделан — каждая строка раскрывается по тапу (▸/▾), самое свежее событие открыто; детали второй строкой + кто/когда, дата и время раздельно. Точка «edit» с «было→стало»: markOrder снимает ключевые поля (sum/date/t1/t2/address/worker/client), window.save сравнивает и пишет logAction edit с перечнем «поле: было → стало» (кроме случаев, где edit пишется явно — доп. работы).
- **Баг 3 (модалки закрываются сами)**: причина — модалки вставлялись в #app, который render() уничтожает при снапшоте/таймере. Исправлено: контейнер #modal-root вне #app; openModal()/closeModal()/modalIsOpen(); payModal, closeOrderModal, conflictModal, cancelOrderModal идут в modal-root; кнопки «Отмена» и клик по фону — closeModal(). Снапшоты/таймеры/бейджи модалки не трогают, ввод сохраняется.
- Контрольный чек (node vm): 21/21 OK — доступ, права (orders_history/orders_cancel/finance_edit), специализации (admin+staff), профиль/PIN, заявки/тестовые, закрытие с фотоотчётом, финансы/CSV, отмена, edit было→стало, модалки modal-root (открытие/закрытие/отмена), схема, уведомления, напоминания, офлайн-очередь, история, бейджи.

### 21.09.2026 — Демо-заявки только для разработчика
- Сид заявок удалён из defaultData (orders:[]) — новая установка стартует пусто; сид ролей/шаблонов и bootstrap владельца не тронуты.
- visOrder: test-заявки скрыты ВСЕГДА и ВЕЗДЕ; исключение — владелец/админ (adminLike) при DB.showTest=true (тумблер №41) — с бейджем 🧪. bizOrder/уведомления/напоминания — test пропускаются всегда.
- Разовая пометка в проде (markDemoOrdersOnce, только владелец, флаг localStorage crm_demo_marked): совпадение клиент+адрес+сумма с сигнатурами удалённого сида (Иванов/Петров/Сидоров/Козлова/Николаев — 5 шт.) → test:true, других изменений нет.
